---
layout: "layouts/base"
css: "css/home.css"
pagination:
  data: collections.articles
  size: 10
  reverse: true
  alias: articles
---
<main class="main-wrapper">
  <section class="home-container">
    <div class="article-container new-article">
      <h3 class="new-article-size">Já possuímos <span>{{ articles.size }} artigos publicados</span> em nossa plataforma!</h3>
      <p>Se mantenha atualizado e compartilhe conhecimento!</p>
      <a href="https://github.com/carlos8v/md-talk">Postar agora</a>
    </div>
    <h2>Últimos artigos:</h2>
    {% for article in articles %}
      <div class="article-container">
        <a class="article-title" href="{{ domain }}{{ article.url }}">{{ article.data.title }}</a>
        <div class="author-container">
          <img src="
            {% if article.data.author.profile %}
              {{ article.data.author.profile }}
            {% else %}
              https://ui-avatars.com/api/?name={{ article.data.author.name }}&background=333&color=fff&size=36
            {% endif %}"
          />
          <strong>{{ article.data.author.name }}</strong>
        </div>
        {% if article.data.tags.size > 1 %}
          <div>
            {% for tag in article.data.tags offset:1 %}
              <span class="article-tag">#{{ tag }}</span>
            {% endfor %}
          </div>
        {% endif %}
      </div>
    {% endfor %}
  </section>
</main>
